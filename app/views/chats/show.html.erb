<div class="container" style="margin-top:15px">
    <div class="row">
      <div class="col-3"></div>
        <div class="col-6">
            <h2 id="room" data-room="<%= @room.id %>" data-user="<%= current_user.id %>"><%= @user.name %> さんとのチャット</h2>

            <table class="message">
              <thead>
                <tr>
                  <th style="text-align: left; font-size: 20px;"><%= current_user.name %><%= attachment_image_tag current_user, :profile_image, size: "30x30", fallback: "no_img.jpg" %></th>
                  <th style="text-align: right; font-size: 20px;"><%= @user.name %><%= attachment_image_tag @user, :profile_image, size: "30x30", fallback: "no_img.jpg" %></th>
                </tr>
              </thead>
              <% @chats.each do |chat| %>
                <% if chat.user_id == current_user.id %>
                <tbody>
                  <tr>
                    <th>
                      <p style="text-align: left;"><%= chat.message %></p>
                    </th>
                    <th></th>
                  </tr>
                </tbody>
                <% else %>
                <tbody>
                  <tr>
                    <th></th>
                    <th>
                      <p style="text-align: right;"><%= chat.message %></p>
                    </th>
                  </tr>
                </tbody>
                <% end %>
              <% end %>
            </table>

            <%= form_with model: @chat do |f| %>
              <%= f.text_field :message %>
              <%= f.hidden_field :room_id %>
            <% end %>
        </div>
        <div class="col-3"></div>
    </div>
</div>